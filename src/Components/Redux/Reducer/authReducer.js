const  initialstate={
     isAthenticatedd:false,
     usser:{}
}

 export default function(state=initialstate,action){

     switch(action.type){

        default:{
            return state;
        }
     }

 }